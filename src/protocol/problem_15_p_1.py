from protocol.errors import EOPINotImplementedError


class Problem15P1(object):
    # all n rings are assumed to start on Peg 0 and all the rings must be moved to Peg 1
    # where the pegs are numbered Peg 0, Peg 1, Peg 2
    def towers_of_hanoi(self, n_rings):
        raise EOPINotImplementedError()


